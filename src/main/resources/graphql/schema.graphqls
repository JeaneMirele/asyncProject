type Mutation {
    solicitarRelatorio(request: RelatorioRequestInput!): String!
}

input RelatorioRequestInput {
    usuario: String!
    tipoRelatorio: String!
}

type Query {
    consultarStatus(protocolo: String!): String!
}